version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGODB_URI=mongodb+srv://qltvtvu:qltvtvu@qltv.kty1ofc.mongodb.net/library_db?retryWrites=true&w=majority
      - JWT_SECRET=your_jwt_secret_key
      - CLIENT_URL=http://localhost:3000
    restart: always

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - server
    restart: always
